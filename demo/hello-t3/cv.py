import cv2
import math
import time
import numpy

def checkAnswer(val): #the main function to check the outputted result on the calculator
    #open the camera
    #take in a frame
    #de-fish-eye
    #search for and pick out the numbers
    #evaluate the numbers
    pass

